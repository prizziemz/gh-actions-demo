on:
  workflow_dispatch:
job:
  build:
    step:
      - name: Checkout source code
        user: action/checkoout@v7.0.0
        with:
          repository: ${{github.repository}}
          ref: ${{github.ref_name}}
      - name: List files in workspaces
        run: ls ${{github.workspace}}   
